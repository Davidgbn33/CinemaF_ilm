{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Movies</h1>

    {% for movie in movies %}
        <h3>{{ movie.title }}</h3>
        <p>{{ movie.description }}</p>
    {% endfor %}
{% endblock %}
